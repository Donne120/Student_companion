# Copy to .env and fill in your Firebase values.
# These must match the Firebase project used by the web app.

# React Native Firebase reads google-services.json (Android) and
# GoogleService-Info.plist (iOS) placed in the project root.
# You do NOT need to set Firebase keys here — they come from those files.
# See: https://rnfirebase.io/

# Backend (same as web app)
API_URL=https://studentcompanion-alu-chatbot.hf.space
